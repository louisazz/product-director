# Memory index

- `长期认知.md`：人性与群体行为、需求从哪来、产品作为系统、演化与规划、结构与抽象。
- `产品判断.md`：具体的产品判断，每条含走偏与真实案例（含取舍）。定义、验证、需求、结构、做减法、交互、选择权、增长、界面与文案用词。
- `用户偏好.md`：语言、分析深度与表达方式。

这里只维护简短入口。需要具体内容时，请按当前任务使用 `read` 读取对应文件。
